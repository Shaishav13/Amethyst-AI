# Amethyst AI Assistant - Dependencies
# =====================================

# Core AI
ollama>=0.3.0

# API Server (Angular backend)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9    # for file uploads

# UI Framework (legacy desktop — kept for fallback)
customtkinter>=5.2.2
Pillow>=10.0.0

# Voice - STT (offline)
SpeechRecognition>=3.10.0
pyaudio>=0.2.14
vosk>=0.3.45
pyttsx3>=2.90

# IDE Bridge / Hotkeys
keyboard>=0.13.5
pyperclip>=1.8.2
pywin32>=306

# Document Parsing
pymupdf>=1.24.0

# RAG / Vector Database
chromadb>=0.5.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0

# Tier 3 Power Features
psutil>=5.9.0          # System monitoring (CPU/RAM/disk/battery)
# For Vision: ollama pull llava  (no pip install needed)

# Async & Utilities
httpx>=0.27.0

# Code syntax highlighting
pygments>=2.17.0

langchain-ollama

langchain-chroma
